/**
 * styled component types
 */
export type TTextStyled = {
    // font-weight
    small?: boolean;
    medium?: boolean;
    large?: boolean;
    title?: boolean;

    // text-align
    center?: boolean;
    right?: boolean;

    // font-weight
    light?: boolean;
    havy?: boolean;
    bold?: boolean;

    color?: string;
    margin?: string;
    padding?: string;
};
